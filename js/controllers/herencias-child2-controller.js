angular.module('app')
    .controller('HerenciasChild2Controller', ['$scope', function($scope) {

//todo por que la vista no carga cuando la pongo como nested?
    	//$scope.childName='cotoPelaito';
    	console.log($scope);

    }]);
